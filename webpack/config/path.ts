import { resolve } from 'path'

export const BASE_DIR = resolve(__dirname, '../../')
export const LIB_DIR = resolve(BASE_DIR, 'lib')
export const SRC_DIR = resolve(BASE_DIR, 'src')
export const DEV_DIR = resolve(BASE_DIR, 'dev')
export const TSCONFIG_DIR = resolve(BASE_DIR, 'tsconfig')
